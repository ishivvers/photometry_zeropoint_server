These are the modeled spectra constructed by pysynphot from 
K&C models.  Saved as numpy arrays, with temperature, metallicity, and log_g
in the filenames.

Example:
5000K_00Z_45g.npy  -> T=5000K, Z=0, log_g=4.5

> spec = np.load('5000K_00Z_45g.npy')
> wl_angstroms = spec[0]
> flux_Flam = spec[1]
